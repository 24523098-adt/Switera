---
phase: 260702-p1v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/services/distribusiService.js
  - server/src/services/publicService.js
  - server/src/routes/publicRoutes.js
  - server/src/index.js
  - src/pages/Landing.jsx
autonomous: true
requirements:
  - QUICK-260702-p1v
must_haves:
  truths:
    - "Landing menampilkan ranking kota + peta distribusi dari data DB (via GET /public/landing-stats), bukan dari file seed statis."
    - "Landing tetap tampil (tanpa crash/blank) meski backend/endpoint gagal — state fallback kosong + tetap render peta kosong."
    - "Endpoint /public/landing-stats dapat diakses TANPA JWT (benar-benar publik, tanpa requireAuth)."
    - "Tidak ada lagi import domain-data statis (src/data/*.json) di kode aplikasi src/ (pages/components)."
  artifacts:
    - server/src/services/publicService.js
    - server/src/routes/publicRoutes.js
    - server/src/services/distribusiService.js
    - server/src/index.js
    - src/pages/Landing.jsx
  key_links:
    - "publicRouter dipasang di server/src/index.js DI ATAS errorHandler dan TANPA requireAuth."
    - "Landing memanggil apiFetch('/public/landing-stats', { auth: false }) — tanpa Authorization header, aman pre-login."
    - "Bentuk data { ranking, daftarKota } cocok persis dengan props PetaGeografis (ranking: [{kota,totalPermintaan}], daftarKota: [{nama,kapasitas}])."
---

<objective>
Menghapus semua dummy/static domain data dari frontend Landing.jsx dan menggantinya dengan data nyata dari PostgreSQL (via Prisma), tanpa mengubah desain/layout/UX halaman marketing.

Konkretnya: (1) tambah endpoint publik backend TANPA auth `GET /public/landing-stats` yang mengembalikan agregat demo (ranking kota berdasarkan total permintaan + daftar kota beserta kapasitas) dibaca dari DB via service layer yang sudah ada; (2) ubah `src/pages/Landing.jsx` agar mengambil data dari endpoint publik itu, menggantikan konstanta statis `kotaDemoSeed` dan import seed JSON; (3) verifikasi tidak ada dummy/static domain data lain tersisa di `src/`.

Purpose: Halaman Landing (tampil SEBELUM login, tanpa JWT) harus menampilkan data nyata dari DB, bukan angka seed yang di-hardcode — sesuai permintaan user, dan sesuai core value "app terasa lengkap & tepercaya end-to-end". Endpoint HARUS publik karena Landing tidak boleh memanggil route ber-auth (T-09-LAND-401).
Output: 1 service baru + 1 route publik baru di backend, satu baris export tambahan di distribusiService, dan Landing.jsx yang mengambil data via fetch token-less dengan fallback aman.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@./.claude/CLAUDE.md

# Backend — pola yang WAJIB ditiru (route/service, reuse Prisma via service, mount di atas errorHandler)
@server/src/index.js
@server/src/routes/kotaRoutes.js
@server/src/routes/distribusiRoutes.js
@server/src/services/distribusiService.js
@server/src/services/kotaService.js
@server/src/services/permintaanService.js

# Frontend — file yang diubah + fetch wrapper token-less + konsumen data
@src/pages/Landing.jsx
@src/api/apiClient.js
@src/components/PetaGeografis.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint publik backend GET /public/landing-stats (reuse service layer, tanpa auth)</name>
  <files>server/src/services/distribusiService.js, server/src/services/publicService.js, server/src/routes/publicRoutes.js, server/src/index.js</files>
  <action>
Buat endpoint agregat PUBLIK yang membaca dari DB lewat service yang sudah ada — JANGAN tulis raw Prisma di route/service baru; reuse `getPermintaan`/`getDaftarKota` dan logika ranking yang sudah diport ke backend.

1) `server/src/services/distribusiService.js`: ubah deklarasi `const aggregatePermintaanRanking = ...` menjadi `export const aggregatePermintaanRanking = ...` (HANYA tambahkan kata kunci `export`; JANGAN ubah body-nya — paritas logika dengan src/utils/distribusi.js harus tetap). Ini agar logika ranking dipakai ulang tanpa duplikasi.

2) `server/src/services/publicService.js` (baru): export async `getLandingStats()`. Import `getPermintaan` dari `./permintaanService.js`, `getDaftarKota` dari `./kotaService.js`, dan `aggregatePermintaanRanking` dari `./distribusiService.js`. `Promise.all` `getPermintaan()` + `getDaftarKota()`. Hitung `ranking = aggregatePermintaanRanking(permintaan)`. Map daftarKota ke bentuk minimal `{ nama, kapasitas }` saja (batasi payload publik ke persis yang dibutuhkan widget; JANGAN kembalikan baris permintaan mentah dengan keterangan dsb). Return `{ ranking, daftarKota }`. Tambahkan komentar singkat (istilah domain Bahasa Indonesia) yang menjelaskan bahwa fungsi ini menyediakan agregat demo untuk halaman Landing.

3) `server/src/routes/publicRoutes.js` (baru): tiru struktur `kotaRoutes.js` TAPI TANPA `requireAuth`/`requireRole` sama sekali. `const router = express.Router();` lalu `router.get("/landing-stats", async (req, res, next) => { try { return res.status(200).json(await getLandingStats()); } catch (error) { return next(error); } })`, `export default router`. Beri komentar header (Bahasa Indonesia) yang menjelaskan MENGAPA router ini tidak ber-auth: halaman Landing tampil sebelum login sehingga endpoint agregat demo ini harus publik; data non-sensitif (agregat, tanpa PII).

4) `server/src/index.js`: import `publicRouter` dari `./routes/publicRoutes.js` bersama import router lain, lalu pasang `app.use("/public", publicRouter);` DI ATAS `app.use(errorHandler)` dan TIDAK di bawah requireAuth apa pun (letakkan bersama mount domain lain, mis. setelah baris mount `/activity-log`). CORS sudah dibatasi ke origin Vite dev — GET publik dari origin dev yang sama sudah lolos; JANGAN ubah konfigurasi CORS (hindari over-engineering).
  </action>
  <verify>
    <automated>grep -c "requireAuth" server/src/routes/publicRoutes.js  # harus 0 (membuktikan route publik tanpa auth)</automated>
    <automated>node --input-type=module -e "import('./server/src/services/publicService.js').then(m=>{if(typeof m.getLandingStats!=='function')throw new Error('getLandingStats tidak diekspor');console.log('OK export')})"</automated>
  </verify>
  <done>File publicService.js + publicRoutes.js ada; `aggregatePermintaanRanking` diekspor dari distribusiService; `/public` terpasang di index.js di atas errorHandler tanpa requireAuth. Dengan backend berjalan + DB terseed, `curl -s http://localhost:4000/public/landing-stats` (tanpa header Authorization) mengembalikan 200 JSON berisi kunci `ranking` (array {kota,totalPermintaan}) dan `daftarKota` (array {nama,kapasitas}).</done>
</task>

<task type="auto">
  <name>Task 2: Landing.jsx ambil data dari endpoint publik (ganti seed statis, fallback aman)</name>
  <files>src/pages/Landing.jsx</files>
  <action>
Ganti SUMBER DATA saja — JANGAN ubah desain/layout/markup/UX widget mana pun.

1) Hapus import `permintaanSeed` dari `../data/permintaan.json` dan import `aggregatePermintaanRanking` dari `../utils/distribusi` (tidak lagi dipakai — agregasi kini di backend). Konfirmasi dulu keduanya memang hanya dipakai di titik yang diganti sebelum menghapus.

2) Hapus konstanta inline `kotaDemoSeed` (blok delapan kota beserta kapasitasnya). Perbarui komentar header di atasnya agar mencerminkan kenyataan baru: Landing kini mengambil data ranking/kota demo dari endpoint PUBLIK `/public/landing-stats` (tanpa JWT), menggantikan data statis sebelumnya; pertahankan catatan bahwa Landing tetap tidak boleh memanggil endpoint ber-auth (risiko 401/redirect-loop pre-login). PENTING (disiplin gate): saat menulis ulang komentar, JANGAN menyebut nama identifier/file yang dihapus secara literal — deskripsikan secara konseptual ("data statis sebelumnya"), supaya gate negatif di Task 3 tetap bersih.

3) Import `{ apiFetch }` dari `../api/apiClient`.

4) Ganti dua baris `useMemo` (`rankingDemo`/`daftarKotaDemo`) dengan React state: `rankingDemo`/`setRankingDemo` init `[]`, dan `daftarKotaDemo`/`setDaftarKotaDemo` init `[]`. Nilai awal array kosong = fallback aman default.

5) Tambah satu `useEffect` (mount-only) yang memanggil `apiFetch("/public/landing-stats", { auth: false })`. Flag `{ auth: false }` membuat wrapper TIDAK melampirkan header Authorization — wajib, karena Landing pre-login tanpa JWT. Pada sukses, set kedua state dari `data.ranking` dan `data.daftarKota` (guard dengan `?? []`). Pada gagal (catch), biarkan state tetap array kosong — halaman marketing TIDAK boleh crash/blank; PetaGeografis merender peta kosong dengan aman. Gunakan penjaga pembatalan (mis. flag `let aktif = true` yang di-set false pada cleanup, atau AbortController) agar tidak ada setState setelah unmount.

6) Biarkan pemakaian `<PetaGeografis ranking={rankingDemo} daftarKota={daftarKotaDemo} />` apa adanya — bentuk data identik dengan sebelumnya, jadi tidak ada perubahan props/tampilan.
  </action>
  <verify>
    <automated>grep -c "kotaDemoSeed" src/pages/Landing.jsx  # harus 0</automated>
    <automated>grep -c "data/permintaan.json" src/pages/Landing.jsx  # harus 0</automated>
    <automated>grep -c "auth: false" src/pages/Landing.jsx  # harus >=1 (fetch token-less)</automated>
  </verify>
  <done>Import seed JSON + konstanta kotaDemoSeed terhapus; Landing mengambil ranking/daftarKota via apiFetch token-less dan menyimpannya di state; PetaGeografis menerima data dari state (bukan seed). Dengan backend jalan: peta menampilkan marker dari DB. Dengan backend mati: Landing tetap render tanpa crash (peta kosong). Layout/markup widget tidak berubah.</done>
</task>

<task type="auto">
  <name>Task 3: Verifikasi tidak ada dummy/static domain data lain tersisa di src/</name>
  <files>src/</files>
  <action>
Verifikasi bahwa tidak ada import/konstanta domain-data statis lain yang tersisa di kode aplikasi frontend.

1) Pindai `src/` untuk: (a) import file `src/data/*.json`, dan (b) konstanta seed domain inline yang memberi makan widget domain nyata. Setelah Task 2, tidak boleh ada `src/pages/**` atau `src/components/**` yang meng-import `../data/*.json`.

2) Konfirmasi bahwa satu-satunya pemakai `src/data/*.json` yang tersisa berada DI LUAR kode aplikasi src/ — yaitu `server/prisma/seed.js` (seeding DB) yang sah dan DI LUAR scope; JANGAN diubah.

3) EKSPLISIT DI LUAR SCOPE dan TIDAK BOLEH diubah: angka ilustrasi marketing dekoratif di dalam `FiturVisual` di Landing.jsx (baris mockup statis di section fitur, mis. "287 ton"). Itu grafis mockup UI statis, BUKAN pembacaan domain-data dari DB, dan constraint melarang mengubah desain visual Landing. Jangan disentuh. Jika menemukan import domain-data statis nyata yang lain di kode aplikasi, LAPORKAN (jangan diam-diam memperluas scope atau mengubah UI tanpa konfirmasi).
  </action>
  <verify>
    <automated>grep -rEc "from ['\"]\.\.?/(\.\./)?data/[^'\"]*\.json" src/pages src/components | grep -v ':0$' || echo "CLEAN: tidak ada import data/*.json di pages/components"</automated>
  </verify>
  <done>Tidak ada import `../data/*.json` di `src/pages` maupun `src/components`. Pemakaian seed JSON yang tersisa hanya di `server/prisma/seed.js` (di luar scope). Ilustrasi marketing dekoratif FiturVisual dibiarkan utuh. Jika ada temuan lain, dilaporkan alih-alih diubah diam-diam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated visitor → /public/landing-stats | Pengunjung pra-login (tanpa JWT) melintasi batas ke endpoint publik baru |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p1v-01 | Information Disclosure | GET /public/landing-stats | low | accept | Data yang diekspos hanya agregat non-sensitif (ranking total permintaan per kota + kapasitas kota) — sesuai permintaan eksplisit user untuk data nyata di Landing; tanpa PII/kredensial. Diterima untuk demo sekolah. |
| T-p1v-02 | Information Disclosure | publicService.getLandingStats | low | mitigate | Batasi payload: kembalikan hanya `{ ranking, daftarKota:{nama,kapasitas} }` — JANGAN kembalikan baris permintaan mentah (keterangan, tanggal detail) ke jalur publik. |
| T-p1v-03 | Denial of Service | endpoint publik tanpa auth | low | accept | Endpoint read-only tanpa mutasi; risiko flooding pada dev lokal minimal untuk demo sekolah. Rate-limit di luar scope quick task ini (tidak ada lib baru). |
| T-p1v-04 | Elevation of Privilege | mount router di index.js | medium | mitigate | Router publik HANYA mengekspos satu GET agregat; dipasang tanpa requireAuth secara sengaja & terisolasi (file route sendiri), tidak menyentuh router domain ber-auth lain. Gate verify `grep -c requireAuth == 0` khusus di publicRoutes.js memastikan tidak ada route lain diselundupkan ke sana. |
</threat_model>

<verification>
- `grep -c "requireAuth" server/src/routes/publicRoutes.js` → 0 (route publik).
- `getLandingStats` diekspor & mengembalikan `{ ranking, daftarKota }` dengan bentuk yang cocok untuk PetaGeografis.
- `curl -s http://localhost:4000/public/landing-stats` (tanpa Authorization) → 200 JSON dengan kunci `ranking` + `daftarKota` (butuh backend jalan + DB terseed).
- `grep -c "kotaDemoSeed" src/pages/Landing.jsx` → 0; `grep -c "data/permintaan.json" src/pages/Landing.jsx` → 0.
- Manual: buka Landing pra-login dengan backend jalan → peta menampilkan marker dari DB; matikan backend → Landing tetap render (peta kosong, tanpa crash/blank).
- Tidak ada import `../data/*.json` tersisa di `src/pages` / `src/components`.
</verification>

<success_criteria>
- Landing.jsx tidak lagi mengandung `kotaDemoSeed` maupun import `../data/permintaan.json`; datanya berasal dari `/public/landing-stats`.
- Endpoint `/public/landing-stats` publik (tanpa requireAuth), dipasang di atas errorHandler, membaca DB via service layer (tanpa raw Prisma di route), mengembalikan `{ ranking, daftarKota }`.
- Fetch di Landing token-less (`auth: false`) dengan fallback array kosong bila gagal — halaman marketing tidak pernah crash/blank.
- Desain/layout/UX widget Landing tidak berubah — hanya sumber data yang diganti.
- Tidak ada dummy/static domain data lain tersisa di kode aplikasi `src/`.
</success_criteria>

<output>
Buat `.planning/quick/260702-p1v-hapus-dummy-static-data-dari-frontend-sw/260702-p1v-SUMMARY.md` saat selesai.
</output>
