// Valid account roles, mirroring src/utils/navigation.js's roleOptions on
// the frontend. Kept as a small server-side copy rather than importing
// across the frontend/backend project boundary (server/ is a fully
// independent Node.js project, see Phase 6 Plan 1).
export const roleOptions = ["Admin", "Manajer Distribusi", "Tim Logistik"];
